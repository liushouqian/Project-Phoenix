# Day 6

## Sprint
Sprint 1 - AI Workspace Foundation

## Sprint Task
Task 4 - Chat Service Hardening

## Today's Goal

## Finished

## Problems

## What I Learned

## Reflection

## Tomorrow

## remark
我弄了一个DeepSeek的api, 我已经自己更换成DeepSeek的模型了, 付费模型可以正式使用了. 

测试1: 中文问题

{

  "reply": "前端工程师转型 AI 工程师，最重要的三个能力是：\n\n1. **数学基础**（线性代数、概率统计、微积分）  \n   - 理解向量、矩阵运算、梯度下降、贝叶斯等概念，否则看模型论文和公式会非常吃力。\n\n2. **Python 编程与数据处理能力**  \n   - 熟练使用 NumPy、Pandas、Scikit-learn 等工具进行数据清洗、特征工程，并掌握 Jupyter Notebook 的交互式开发流程。\n\n3. **机器/深度学习模型的理解与实战**  \n   - 能从算法原理（监督/非监督/强化学习）到框架（PyTorch/TensorFlow）落地一个完整项目，包括训练、评估、调优和部署。\n\n你的前端背景在**模型可视化、交互式 Demo 构建、工程化部署（如 Web端推理）**上反而是独特优势，可以善加利用。"

}

测试2: 英文问题

{

  "reply": "Here’s a practical 3-step roadmap for a frontend engineer to master FastAPI:\n\n---\n\n### 1. **Async Python & Pydantic (Week 1)**\n- **Async/Await**: Learn Python’s `async def` and `await` – same concept as JavaScript but with `import asyncio`.  \n- **Pydantic Models**: Define request/response models using Python type hints (similar to TypeScript interfaces).  \n  ```python\n  from pydantic import BaseModel\n\n  class Item(BaseModel):\n      name: str\n      price: float\n  ```\n- **Goal**: Write a FastAPI endpoint that accepts and validates a JSON body using Pydantic.\n\n---\n\n### 2. **Build a Minimal CRUD API (Week 2)**\n- **Path & Query Params**: Map URL parts and query strings to function arguments (like Express/Next.js API routes).  \n- **Dependency Injection**: Use `Depends()` for reusable logic (e.g., DB sessions, auth checks) – cleaner than middleware.  \n- **Interactive Docs**: FastAPI auto-generates Swagger UI at `/docs` – test your API visually.  \n- **Goal**: Create a complete CRUD API for a single resource (e.g., `/items`) without a database (in‑memory list).\n\n---\n\n### 3. **Database & Auth Integration (Week 3)**\n- **SQLModel or SQLAlchemy**: Use an ORM to persist data. SQLModel is Pydantic + SQLAlchemy combined.  \n- **JWT Authentication**: Implement login endpoint returning a token, protect routes with OAuth2PasswordBearer.  \n- **CORS & Deployment**: Configure `CORSMiddleware` (familiar frontend issue) and deploy to Railway/Render.  \n- **Goal**: Deploy a full‑stack app with your frontend (React/Vue) talking to FastAPI with JWT auth.\n\n---\n\n**Pro tip**: Leverage your frontend knowledge – FastAPI’s async DNA, type‑safe models, and dependency injection will feel natural after TypeScript and React hooks."

}

测试3: 空message

{

  "reply": "Message cannot be empty."

}