# Day 7

## Sprint
Sprint 1

## Sprint Task
Task 5 - Conversation Management
Task 5 — 多轮会话记忆（Session-based Conversation Memory）

## Today's Goal
Add in-memory conversation history support for /chat API.

## Finished
- Added session_id to ChatRequest
- Updated /chat route to pass session_id and message into generate_reply
- Implemented in-memory conversation storeage with conversation_store
- Added get_history(session_id) and append_message(session_id, role, content)
- Injected history into LLM request messages
- Verified multi-turn conversation works with the same session id
- Confirmed the assistant can remember user name and city across turns

## Problems
- At first, I hit a NoneType-related issue when trying to inject conversation history into the model input.
- I also ran into several model access issues while testing free endpoints, including 404 and 429.
- During testing in Swagger, I initially got confused by request body changes and response behavior, which made debugging slower.
- Swagger UI was misleading during testing. It sometimes looked like the response was not updated, but curl confirmed the backend behavior was correct.
- I learned that API debugging should not rely only on Swagger UI. Terminal logs + curl are more trustworthy for backend verification.
- I also hit model/provider issues earlier in Task 5, including rate limits and unavailable free models, and finally switched to a paid DeepSeek model to stabilize the workflow.

## How I Solved Them
- I changed the history getter to return an empty list by default instead of None.
- I switched to a paid DeepSeek-compatible API setup to make the chat service stable.
- I added debug logs for session_id, message, and history before/after, then verified the full conversation flow step by step.

## What I Learned
- route is responsible for HTTP request/response handling.
- service is responsible for business logic such as model calling and history assembly.
- Conversation memory can be implemented by storing messages under a session_id.
- Debugging AI applications often means checking data flow, not just checking the model ouput.
- Multi-turn chat is essentially "message history + model call + history persistence".
- `session_id` is the key that groups messages into one conversation.
- Backend memory is not “magic memory”; it is just structured conversation history stored by session.
- Swagger UI is useful for quick checks, but curl is a more reliable way to verify backend behavior.
- Route, schema, service, and store each have different responsibilities in a backend application.

## Reflection

Today was the first day I felt that this backend was becoming a real AI application instead of just a simple API demo.
The biggest gain was not only making /chat work, but understanding how conversation memory is actually wired into the system.
I also felt the pain of real engineering work: environment issues, model availability, request validation, and That was tiring, but it also made the progress feel more solid.

## Tomorrow
- Refactor and clean the current chat backend code
- Improve configuration and model error handling
- Start the next task of Sprint 1

## What I Finished
- Added 'session_id' to 'ChatRequest'
- Implemented in-memory conversation history with 'conversation_store'
- Updated 'chat_service' to load history before LLM call
- Appended both user message and assistant reply back into history
- Verified multi-turn memory using curl with the same 'session_id'

## Verification
Used curl to test 3 rounds under the same session_id:

1. `我叫BoGui`
2. `我住在上海`
3. `我叫什么，住在哪里？`

Result: 
- The assistant correctly remembered the user's name and city.
- This confirmed that conversation history was being stored and passed into the model successfully.